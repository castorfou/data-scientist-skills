ri est un dataframe

value_counts(): counts the unique values in a Serie
	
	ri.stop_outcome.value_counts()
	
	ri.stop_outcome.value_counts(normalize=True)
		express counting as proportions

Filtering by multiple conditions

	ri[(ri.driver_gender == 'F') & (ri.is_arrested == True)]
	pour un or c'est |
	
Mean sur une colonne de boolean

	ri.is_arrested.mean()
	c'est pareil que ri.is_arrested.value_counts(normalize=True) pour la valeur True

Comparing groups using groupby

	ri.groupby('district').is_arrested.mean()
	
grouping by multiple categories

	ri.groupby(['district','driver_gender']).is_arrested.mean()
	
# Check if 'search_type' contains the string 'Protective Frisk'
ri['frisk'] = ri.search_type.str.contains('Protective Frisk', na=False)
	
	